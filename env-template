# AWS S3 Configuration (for client tools only)
AWS_ACCESS_KEY_ID=<key>
AWS_SECRET_ACCESS_KEY=<secret>
S3_TRANSCRIBER_BUCKET=<your-transcriber-bucket>
S3_TRANSCRIBER_PREFIX=<your-prefix>
AWS_REGION=us-east-1

# Optional: Custom S3 endpoint
S3_ENDPOINT_URL=<your-s3-endpoint-url>

# Transcription Service Configuration
OLLAMA_URL=<your-ollama-url>

# Optional: Deployment Configuration
NOMAD_ADDR=<your-nomad-addr>
NOMAD_DATACENTER=<your-nomad-datacenter>
VAULT_ADDR=<your-vault-addr>

# NOTE: Secrets (AWS credentials, HF tokens) are handled by Vault in production
# These should NOT be stored in .env files and are injected by Nomad at runtime
